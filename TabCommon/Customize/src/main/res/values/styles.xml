<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/md_black</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="SplashTheme" parent="AppTheme.NoActionBar">
    </style>

    <style name="MainActivity" parent="AppTheme.NoActionBar">
        <item name="colorAccent">@color/ios_green</item>
    </style>

    <style name="PhotoActivity" parent="AppTheme.NoActionBar">
        <item name="colorPrimary">@color/background_dark</item>
        <item name="colorPrimaryDark">@color/background_photo</item>

    </style>


    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!--<item name="android:textColorPrimary">@color/md_white</item>-->
        <!--<item name="actionMenuTextColor">@color/md_white</item>-->
        <!--<item name="colorAccent">@color/primary_water</item>-->
        <!--<item name="android:background">?attr/colorPrimary</item>-->
        <item name="contentInsetLeft">0dp</item>
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetRight">0dp</item>
        <item name="contentInsetEnd">0dp</item>
    </style>

</resources>